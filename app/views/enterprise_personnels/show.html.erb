<div class="list">
  <span class="show_title">
    <span style="margin-right: 5px;">
      <!--
        <svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" fill="currentColor" class="bi bi-file-earmark-text" viewBox="0 0 16 16">
        <path d="M5.5 7a.5.5 0 0 0 0 1h5a.5.5 0 0 0 0-1h-5zM5 9.5a.5.5 0 0 1 .5-.5h5a.5.5 0 0 1 0 1h-5a.5.5 0 0 1-.5-.5zm0 2a.5.5 0 0 1 .5-.5h2a.5.5 0 0 1 0 1h-2a.5.5 0 0 1-.5-.5z"/>
        <path d="M9.5 0H4a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4.5L9.5 0zm0 1v2A1.5 1.5 0 0 0 11 4.5h2V14a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1h5.5z"/>
        </svg>
      -->
      <%= image_tag "enterprise.png" %>
    </span>
    基本信息
  </span>
  <div class="each-row">
    <%= render @enterprise_personnel %>
  </div>
  <div style="margin-top: 100px; text-align: right; margin: 50px 400px 190px 180px;">
    <% if current_manager.email.include?('enterprise') || current_manager.email.include?('admin')%>
      <%= button_to "删除", @enterprise_personnel, method: :delete, method: :delete, form: { data: { turbo_confirm: '确定删除?' } }, class: "btn btn-danger", style: 'width: 200px; float: right;' %>
    <% end %>
    <% if current_manager.email.include?('enterprise') %>
      <%= link_to '编辑', edit_enterprise_personnel_path(@enterprise_personnel), style: 'text-decoration: none; color: #000;' %> |
    <% end %>
    <%#= link_to '< 返回', enterprise_personnels_path, style: 'text-decoration: none; margin-left: 100px; font-size: 20px;' %>
    <%= link_to '返回', enterprise_personnels_path, class: 'btn btn-primary', style: 'width: 150px; float:left; background-color: white; color: #0d6efd;' %>
  </div>
</div>
<style>
.list {
  background: #ffffff;
  padding: 20px;
  height: 700px;
}
.each-row {
  border-radius: 5px;
  margin-top: 30px;
  background: white;
  padding: 10px;
}
.show_title {
  font-size: 20px;
  font-family: PingFangSC-Regular, PingFang SC;
  font-weight: 400;
  color: rgba(0,0,0,0.9);
  line-height: 28px;
}
</style>
